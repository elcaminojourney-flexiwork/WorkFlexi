/**
 * Re-export so that app/employer/timesheet/* and other deep routes
 * can import via ../../../components/ConstitutionalScreen (resolving to app/components).
 */
export { default, PanelPurple, PanelBlue, CardWhite } from '../../components/ConstitutionalScreen';
